//  add 함수 만들기 a,b

const add = (a: number, b: number): number => {
  return a + b;
};
// minus,multply 함수 만들기 ts로 컴파일까지

const minus = (c: number, d: number): number => {
  return c - d;
};

const multply = (e: number, f: number): number => {
  return e * f;
};
